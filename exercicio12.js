/*
12) Faça um algoritmo que calcule o fatorial de um número.
*/

const num1 = 4;
const num2 = 7 ;

function numeroFatorial(numFatorial){
    return Math.pow(numFatorial,numFatorial);
}

console.log(numeroFatorial(num1));
console.log(numeroFatorial(num2));